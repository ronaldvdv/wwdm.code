version: '3'
services:
  frontend:
    build: ./src/frontend
    ports:
      - '5002:80'
    depends_on:
      - graphql
    expose:
      - 5002
  graphql:
    build:
      context: ./src/WWDM
      dockerfile: WWDM.GraphQL.Server/Dockerfile
    ports:
      - '5001:80'
    depends_on:
      - db
    expose:
      - 5001
  db:
    build: 
      context: ./data/
      dockerfile: db.Dockerfile
    ports:
      - '3306:3306'
    expose:
      - 3306